# BLE Boat Tracking System Requirements
# Multi-beacon database-backed tracking system

# Core dependencies
flask>=2.3.0
flask-cors>=4.0.0
bleak>=0.21.0
requests>=2.31.0

# Database (sqlite3 is built into Python 3, no need to install)
# Optional: PostgreSQL support
# psycopg2-binary>=2.9.0

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Optional: WebSocket support for real-time updates
# flask-socketio>=5.3.0

# Optional: Better logging
# structlog>=23.1.0
